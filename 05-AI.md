# IA

## Ollama

Versão validada no ambiente: `0.12.6`.

Recursos detectados:

- CUDA
- Flash Attention
- modo de baixa VRAM

### Modelos instalados

- `qwen3:8b`
- `qwen3-coder:30b`
- `nomic-embed-text`

### Estratégia de uso

#### `qwen3:8b`

Modelo recomendado para uso cotidiano:

- conversas gerais;
- Linux e Docker;
- automações;
- busca web;
- RAG;
- tarefas rápidas de programação.

#### `qwen3-coder:30b`

Reservado para tarefas mais complexas de código e arquitetura. No hardware atual, somente parte das camadas cabe na GPU; a maior parte da inferência ocorre em CPU.

### Benchmark registrado

Teste direto no Ollama:

```bash
time docker exec -it ollama ollama run qwen3-coder:30b
```

Prompt:

```text
Explique em poucas palavras o que é Docker
```

Resultado total:

```text
real 0m42.126s
```

O teste demonstrou que o modelo responde diretamente pelo Ollama em cerca de 42 segundos. Uma demora próxima de 10 minutos observada no Open WebUI aponta para processamento adicional na interface, ferramentas ou integração, e não para falha básica do Ollama.

## Open WebUI

Versão validada: `0.10.2`.

- Migrações de banco concluídas sem erro.
- A arquitetura atual usa Workspace Models, Knowledge, Prompts, Skills e Tools.
- Não há uma área separada chamada Assistants.

### Problema em investigação

O Open WebUI apresentou chamadas de ferramenta inadequadas e latência muito superior ao acesso direto ao Ollama com o `qwen3-coder:30b`. O diagnóstico deve comparar simultaneamente os logs de `open-webui` e `ollama` durante a mesma requisição.

## ComfyUI

Esta seção é o inventário permanente do que está instalado dentro do ComfyUI. Testes, erros, medições e comparações devem ser registrados no `10-LAB.md`.

### Versões e ambiente

| Componente | Versão/estado |
|---|---|
| ComfyUI | `0.28.0` |
| Frontend | `1.45.21` |
| PyTorch | `2.13.0+cu130` |
| CUDA do container | `13.0` |
| GPU | NVIDIA GeForce GTX 1650 Max-Q |
| VRAM detectada | aproximadamente 3,7 GB |
| DynamicVRAM | habilitado |
| Offload assíncrono | habilitado |

### Acesso e gerenciamento

- Porta direta: `8188`
- Proxy local: `http://comfy.home`
- Gerenciamento de extensões e modelos: ComfyUI Manager integrado

### Custom Nodes instalados

| Componente | Versão/commit | Finalidade | Estado |
|---|---|---|---|
| ComfyUI Manager | integrado à imagem atual | gerenciamento de custom nodes e modelos | funcional |

Ao instalar novos custom nodes, registrar nesta tabela o nome exato, a versão ou commit, a finalidade e o estado validado.

### Modelos instalados

#### Checkpoints

| Arquivo | Família/tipo | Tamanho aproximado | Diretório | Estado |
|---|---|---:|---|---|
| `sd_xl_turbo_1.0_fp16.safetensors` | SDXL Turbo | 6,5 GB | `~/homelab/compose/comfyui/models/checkpoints` | funcional |

#### LoRAs

Nenhum LoRA registrado até o momento.

#### VAEs

Nenhum VAE adicional registrado até o momento.

#### ControlNet

Nenhum modelo ControlNet registrado até o momento.

#### Outros modelos

Registrar aqui modelos de upscale, embeddings, CLIP, UNet, diffusion models, text encoders e modelos GGUF que forem adicionados.

### Workflows validados

| Workflow | Modelo principal | Resultado | Observações |
|---|---|---|---|
| SDXL Turbo text-to-image | `sd_xl_turbo_1.0_fp16.safetensors` | funcional | primeira imagem gerada em aproximadamente 5 segundos |

### Configurações relevantes

- Modelos persistidos fora do container.
- DynamicVRAM habilitado.
- Offload assíncrono habilitado.
- Usar `--disable-cuda-malloc` somente se surgirem erros CUDA compatíveis com o aviso observado.

### Estratégia para o hardware atual

- Priorizar SDXL Turbo e modelos quantizados.
- Testar resoluções maiores de forma gradual.
- Usar offload para RAM/CPU em modelos que excedem a VRAM.
- Evitar assumir que modelos grandes serão estáveis apenas porque carregam.
- Registrar benchmarks detalhados no `10-LAB.md`.

## Busca web

- Mecanismo: SearXNG.
- Endpoint interno: `http://searxng:8080/search`.
- API JSON validada.

## RAG

- Modelo de embedding instalado: `nomic-embed-text`.
- Banco vetorial: Qdrant.
- Configuração funcional completa de RAG ainda precisa ser validada e documentada.
